import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main:"#00263e",
            light: "#FFFDD0"
    },
    secondary: {
        main: "#ffa472",
        light: "#ffc99d"

    }
}
})